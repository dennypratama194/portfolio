# Block direct HTTP access to sensitive files (dot-files, JSON config, logs)
<FilesMatch "(^\.|\.(json|log)$)">
  Order allow,deny
  Deny from all
</FilesMatch>

# Block the logs directory entirely (contains buyer emails)
RedirectMatch 404 ^/api/logs/
